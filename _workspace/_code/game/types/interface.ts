export interface organAdjustment {
	sens?: number; // sens level
	size?: number; // size blevel
	sizeLv?: number; // size level
	d?: number; //diameter or width
	l?: number; //length or depth
	trait?: string | string[]; //trait
}
